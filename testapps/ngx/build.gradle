idea {
    module {
        excludeDirs += file('node_modules')
        excludeDirs += file('dist')
        excludeDirs += file('lib')
    }
}

apply plugin: TolgeeNodePlugin

tolgeeNpm {
    library = false
    linkablePackages = ["@tolgee/core", "ngx-tolgee"]
}

